import { AdvanceSearchPipe } from './advance-search.pipe';

describe('AdvanceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvanceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});

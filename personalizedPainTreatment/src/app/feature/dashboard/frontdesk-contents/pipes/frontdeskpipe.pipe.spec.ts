import { FrontdeskpipePipe } from './frontdeskpipe.pipe';

describe('FrontdeskpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FrontdeskpipePipe();
    expect(pipe).toBeTruthy();
  });
});
